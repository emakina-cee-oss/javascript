// ESLint Rules provided by eslint-plugin-promise
// https://www.npmjs.com/package/eslint-plugin-promise

module.exports = {
    plugins: [
        'promise'
    ],

    rules: {
        'promise/param-names': 'error'
    }
};
