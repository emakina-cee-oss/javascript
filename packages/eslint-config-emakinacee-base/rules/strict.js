// ESLint Rules related to Strict Mode
// http://eslint.org/docs/rules/#strict-mode

module.exports = {
    rules: {
        // babel inserts `'use strict';` for us
        strict: ['off', 'never']
    }
};
